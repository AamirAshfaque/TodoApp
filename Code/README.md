# Java‑Todo Test Suite

## Setup

Install dependencies:

backend

cd backend && npm install
cd backend && node app.js

frontend

cd frontend && npm install
cd frontend && npm start

Run All Tests

mvn test